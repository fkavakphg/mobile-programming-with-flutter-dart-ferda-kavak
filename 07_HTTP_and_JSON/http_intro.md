# HTTP & JSON in Flutter

In this module you will:
- Add the `http` package to your project
- Fetch JSON from an API
- Decode JSON into Dart objects (maps/lists)
- Render results in a `ListView`
- Handle loading & error states
